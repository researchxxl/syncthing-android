### Use per folder and device custom sync conditions

This feature may be improved in the future. For now, we didn't want to break existing configs and app behaviour. That's why custom sync conditions for a certain network type can only be set if you enable syncing on the network in the global run conditions first.
